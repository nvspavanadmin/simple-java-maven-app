pipeline{
    agent any
    stages{
        stage('build stage'){
            steps{
                sh 'mvn install'
            }
        }
        stage('code scan'){
            steps {
              withSonarQubeEnv('mysonarqube') {
                sh 'sonar:sonar'
              }
            }
        }
        
    }
}